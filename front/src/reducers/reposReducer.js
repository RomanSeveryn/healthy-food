const defaultState = {
  items: [],
  isFetching: true,
  count: 0
}

export default function reposReducer(state = defaultState, action) {
  switch (action.type) {
    case value:
      
      break;
  
    default:
      break;
  }
}